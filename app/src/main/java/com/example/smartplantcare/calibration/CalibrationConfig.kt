package com.example.smartplantcare.calibration

/**
 * Single switch for all calibration-only behavior.
 * Set [CALIBRATION_MODE] to false to remove logging UI and extra navigation paths.
 */
object CalibrationConfig {
    // TEMPORARY CALIBRATION CODE
    const val CALIBRATION_MODE = true
}
